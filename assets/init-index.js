/* ============================================================
   Cancer in the Commonwealth — Homepage init
   Extracted from an inline <script> tag on index.html so the
   site's Content-Security-Policy doesn't need to allow
   'unsafe-inline' scripts. Behavior is unchanged.
   ============================================================ */
CIC.initIndexCards();
