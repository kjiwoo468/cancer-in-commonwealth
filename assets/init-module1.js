/* ============================================================
   Cancer in the Commonwealth — Module 1 page init
   Extracted from an inline <script> tag on module1.html so the
   site's Content-Security-Policy doesn't need to allow
   'unsafe-inline' scripts. Behavior is unchanged.
   ============================================================ */
CIC.initModule('module1');
CIC.initAccordions();
CIC.initChoiceActivities();
CIC.initReveals();
CIC.initReflections();
CIC.initTests('module1');
