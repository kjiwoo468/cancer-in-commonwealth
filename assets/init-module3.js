/* ============================================================
   Cancer in the Commonwealth — Module 3 page init
   Extracted from an inline <script> tag on module3.html so the
   site's Content-Security-Policy doesn't need to allow
   'unsafe-inline' scripts. Behavior is unchanged.
   ============================================================ */
CIC.initModule('module3');
CIC.initAccordions();
CIC.initChoiceActivities();
CIC.initReveals();
CIC.initReflections();
CIC.initTests('module3');
